config argos_smd
    bool "Enable argos smd peripheral"
    depends on UART_INTERRUPT_DRIVEN

if argos_smd
    module = argos_smd
    module-str = argos smd
    source "subsys/logging/Kconfig.template.log_config"
endif