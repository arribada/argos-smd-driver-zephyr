/*
 * Copyright (c) 2025 Arribada Initiative
 *
 * SPDX-License-Identifier: Apache-2.0
 */

/*
 * Devicetree overlay for Adafruit Feather nRF52840 - OTA Test
 *
 * Uses uart0 for communication with the Argos SMD module (same as uart_cmd sample)
 * Console is redirected to USB CDC ACM for debugging via RTT
 */

#include <zephyr/dt-bindings/gpio/gpio.h>

/ {
    chosen {
        zephyr,console = &cdc_acm_uart;
        /delete-property/ zephyr,shell-uart;
        /delete-property/ zephyr,uart-mcumgr;
        /delete-property/ zephyr,bt-mon-uart;
        /delete-property/ zephyr,bt-c2h-uart;
    };
};

&uart0 {
    status = "okay";
    current-speed = <9600>;

    argossmd: argos-smd {
        compatible = "arribada,argos-smd-uart";
        label = "ARGOS_SMD";
        /* Wake the module from low power before talking to it.
         * Feather D9 (P0.26) -> STM32 PB3/WKUP3 (same wiring as uart_cmd).
         */
        wakeup-gpios = <&gpio0 26 GPIO_ACTIVE_HIGH>;
    };
};

zephyr_udc0: &usbd {
    compatible = "nordic,nrf-usbd";
    status = "okay";
    cdc_acm_uart: cdc_acm_uart0 {
        compatible = "zephyr,cdc-acm-uart";
        label = "CDC_ACM";
    };
};
